import pickle 
with open('./processedData/CompanyWiseDict.pkl','rb') as fil:
    company_wise_dict = pickle.load(fil)

# find the earliest start date available in all datasets
# find the latest end date available in all datasets



# trim the lists of these dicts so they start and end at the same time

